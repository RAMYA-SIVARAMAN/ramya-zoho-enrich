#include<stdio.h>
#include<string.h>
int main() {
  char a[100];
  int i,c=1,l;
  scanf("%s",a);
  l=strlen(a);
  for(i=0;i<l;i++)
  {
      if(a[i]==a[i+1])
      {
          c++;
      }
      else
      {   printf("%c",a[i]) ;
          printf("%d",c);
          c=1;
      }
  }
}
  
